#include "Student.h"
#include "LinkedList.h"
#include <iostream> 
#include <string>
#include <vector>


using namespace std;
int main() {
    //Declaring variables
    int numStudents;
    string StudentToRemove;

    //Creating a vector to create multiple student object
    cout << "Enter the number of students: ";
    //cin >> numStudents;

    // Validate user input
    while (!(cin >> numStudents) || numStudents <= 0) {
        // If the input is not an integer or is less than or equal to zero
        cout << "Invalid input. Please enter a positive integer: ";

        // Clear the input buffer
        cin.clear();

        // Ignore any remaining characters in the input buffer
        cin.ignore(1000, '\n');
    }

    vector<Student> students(numStudents);

    //Letiing user enter information about each student
    for (int i = 0; i < numStudents; ++i) {
        cout << "Enter details for student " << i + 1 << ":" << endl;
        students[i].InputDetails();
    }

    //Add students from vector to linked list
    for (int i = 0; i < numStudents; ++i) {
        AddStudent(students[i]);
    }

    //Displaying all students that were added to the LinkedList
    cout<<"\n"<<endl;
    DsplayStudents();
    cout<<"\n"<<endl;

    //Dsiplaying amount of students in the school system
    int NumberOfStudents = CountStudents();
    cout<<"You have: "<<NumberOfStudents<<" Students in the school System"<<endl;

    //Ask what user wants to do
    //1 remove a student from the system
    //2 get lifetime data 
    //3 resart lifetime data
    //4 display students currently added to the data
    //5 quit

    //Removing stuents from the system
    cout << "Enter Name of Student that you want to remove from the system:  ";
    cin >> StudentToRemove;
    RemoveStudent(StudentToRemove);

    // Write student data to file
    WriteStudentData();
    cout<<"\n";

    //Display all students in the linked list or in the system
    DsplayStudents();

    cout<<"\n Reading all data from file: \n";
    ReadingStudentData();
};